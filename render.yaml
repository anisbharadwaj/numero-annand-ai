services:
  - type: web
    name: anis-ai-shield
    env: python

    buildCommand: |
      pip install -r backend/requirements.txt

    startCommand: |
      uvicorn backend.app:app --host 0.0.0.0 --port $PORT
