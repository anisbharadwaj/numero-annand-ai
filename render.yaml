services:
  - type: web
    name: anis-ai-shield
    runtime: python

    buildCommand: |
      pip install -r backend/requirements.txt

    startCommand: |
      python -m uvicorn backend.app:app --host 0.0.0.0 --port 10000

    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
